$kind: http-request
url: "{{base_url}}/api/rescue/rescuer/tasks"
method: GET
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: pm.test('Status is 200', () => pm.response.to.have.status(200));
order: 1000
